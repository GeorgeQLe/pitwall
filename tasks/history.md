# History

## 2026-04-27

- Claude menu bar parity tightened: replaced the generic icon-only status item with a Pitwall icon plus live title text, added a Claude-specific top-bar formatter that restores the old rich breakdown shape (session usage, today-vs-daily-pace, weekly usage, reset countdown), and brought back the three ClaudeUsage tray theme families (`Running`, `Racecar`, `F1 Quali`) as a saved `Claude tray theme` display preference persisted across macOS/Windows/Linux configuration stores and onboarding drafts.
- Claude launch behavior tightened: `MenuBarController.loadConfiguration()` now triggers an automatic provider refresh on app load and after configuration saves, and Claude hydrates from the last exact retained history snapshot while refresh is in flight so the menu bar does not regress to a fake configurable/estimated placeholder on startup. The history-backed fallback now recomputes daily budget and today usage so the top bar keeps the daily usage versus pace segment even before the live network refresh finishes.
- Claude detail surfaces tightened: enriched Claude usage rows now explain session/weekly pace deltas and extra-usage dollar consumption, and provider cards/popover copy now expose the weekly pace rationale plus daily budget/today-usage context instead of only the raw action label.
- Validation: `swift build` passes, focused formatter/configuration tests passed during iteration, and final `swift test` passes **240 / 240** with 0 failures and 0 unresolved warnings. `make install` was rerun after the menu bar/startup fixes to rebuild, codesign-verify, reinstall, and relaunch `/Applications/Pitwall.app`.

- Browser-first Codex auth shipped: replaced the old Terminal handoff with an app-owned `codex login --device-auth` flow that streams the CLI output, extracts the verification URL and device code, opens the browser directly, shows live progress/cancel/retry state in Settings and onboarding, waits for CLI completion, and refreshes Codex status automatically. Kept the API-key path headless through `codex login --with-api-key`, tightened passive Codex detection/status merging so CLI-verified auth upgrades the provider state, and normalized failure messaging to avoid surfacing raw device-auth output or secrets. Updated the clean-room spec note to reflect app-orchestrated device auth through the supported CLI.
- Validation: `swift test` passes **236 / 236** with 0 failures and 0 regressions. No build/test warnings were left unresolved.

- Codex auth-mode UX tightened: replaced the free-text auth mode field with a menu picker for Codex in both the onboarding "Connect Codex" step and the provider settings row. The persisted provider profile still stores auth mode as a string, so no storage migration was needed; non-Codex providers keep the existing text-field behavior.
- Onboarding save/test feedback refined: the wizard footer now shows a small progress indicator and busy message while saving Claude credentials and testing the configuration, then returns to the normal footer message once the async work completes.
- Reinstalled the macOS app via `make uninstall` followed by `make install`; `/Applications/Pitwall.app` was rebuilt, ad-hoc signed, verified by `codesign`, copied into `/Applications`, and relaunched. Application Support and Keychain state were preserved by the uninstall path.
- Validation: `swift build` passes, `swift test` passes **222 / 222** with 0 failures and 0 regressions, and `make install` passes with codesign verification and relaunch.

## 2026-04-26

- Claude onboarding workflow unified: removed the separate Save/Test action buttons from the onboarding "Connect Claude" step and made the wizard's existing `Continue` action save the draft credentials, clear the in-memory session key, run the Claude connection check, and advance only when the connection result is acceptable. Settings keeps explicit Save Credentials / Test Connection controls for account maintenance. Added shared credential-draft state so onboarding can drive the form from the footer, added a typed connection-test outcome for blocking auth-expired/missing states, and updated the summary step to reflect accounts saved during the current wizard session.
- Validation: `swift test` passes **222 / 222** with 0 failures and 0 regressions. `git diff --check` passes with no whitespace issues. No warnings emitted by the Swift build/test output.

## 2026-04-25

- Onboarding setup resilience and paste support shipped: installed a minimal macOS app/Edit menu for accessory-mode text editing commands so `Cmd+V` works in Claude credential fields; changed the Claude label field to `Label (optional)` and removed it from the save-required gate, falling back to the org id or `"Claude account"` for display when blank; added `OnboardingDraftStore` to persist only non-secret setup draft state (selected providers, provider plan/auth metadata, preferences, current wizard step) in `UserDefaults`; kept the Claude session key memory-only until `Save Credentials`; and added an onboarding panel close confirmation only when an unsaved session key is present, with forced close after successful setup completion.
- Validation: `swift build` passes, `swift test` passes **222 / 222** with 0 failures, and `make install` passes with ad-hoc signing + relaunch of `/Applications/Pitwall.app`.

- Onboarding Claude credential help animation refined: changed `ClaudeCredentialStepView` so the "How do I find these values?" instructions reveal inside a clipped region below the toggle, with the toggle layered above the moving content. Removed the opacity portion of the transition so the text slides down from the masked top edge instead of popping/fading over the control.
- Validation: `swift build` passes with no warnings emitted.

- Phase 6b Step 6b.5 completed: added the planned Homebrew cask scaffold at `Casks/pitwall.rb` for the GitHub Releases DMG URL pattern (`https://github.com/GeorgeQLe/pitwall/releases/download/v#{version}/Pitwall-#{version}.dmg`) with `sha256 :no_check` as the placeholder until a real signed release exists. Used the idiomatic Homebrew tap path `Casks/` rather than the task's original `Formula/` wording because this is a cask installed via `brew install --cask`. Updated `README.md` with local `make install` guidance and the future self-hosted tap path (`brew tap georgele/pitwall && brew install --cask pitwall`), noting that the public channel depends on the first release and tap prerequisites.
- Validation: `ruby -c Casks/pitwall.rb` passes (`Syntax OK`) and `git diff --check` passes. `brew style --cask Casks/pitwall.rb` could not complete because Homebrew entered its vendor `bundle install` bootstrap and was manually terminated after no lint output; no cask style finding was produced.

## 2026-04-24

- Phase 6b Step 6b.4 completed: added a `make release VERSION=x.y.z` target that validates `VERSION` before invoking `scripts/release.sh`, and created the initial empty Sparkle `appcast.xml` feed skeleton for release automation to append items.
- Validation: `make build` passes with ad-hoc signing, `bash scripts/smoke-install.sh` passes codesign verification, `bash scripts/release.sh --help` parses successfully, and `make release` without `VERSION` fails intentionally with the expected usage error.

- Phase 6b Step 6b.3 completed: added executable `scripts/release.sh` for the public macOS release pipeline. The script accepts `VERSION` as an env var or positional arg, supports `--dry-run`, validates semver-like versions and a clean working tree, loads optional local `.release-config`, builds a Sparkle-configured Developer ID signed app through `scripts/build-app-bundle.sh`, verifies codesign/spctl, creates `build/Pitwall-$VERSION.dmg`, and gates real release-only work behind non-dry-run notarization, stapling, Sparkle EdDSA signing, appcast item append, `gh release create`, and `APPCAST_PUBLISH_COMMAND`. Added `.release-config.example` with placeholder Sparkle/notary/signing values and ignored real `.release-config` in `.gitignore`. Validation: `bash -n scripts/release.sh` passed; `bash scripts/release.sh --help` printed usage; invalid semver exits non-zero as expected; `bash scripts/release.sh 1.0.0 --dry-run` exits at the clean-tree guard as expected because this step's uncommitted files are present; full `swift test` passed **222 / 222** with 0 failures.
- Onboarding Claude credential UX fix shipped: replaced the inert/missable built-in disclosure with an explicit help toggle in `ClaudeCredentialStepView`, added automatic scroll-to-help behavior through `OnboardingWizardView`, and gated wizard advancement on required provider setup values. Claude now requires a saved org id + configured session key before continuing; Codex and Gemini require both plan/profile and auth mode. Reinstalled `/Applications/Pitwall.app` via `make install` after each fix. Validation: `swift build` passed, focused `swift test --filter OnboardingWizardStepSequencerTests` passed 10 / 10, full `swift test` passed **222 / 222** with 0 failures.
- Phase 6b Step 6b.2 completed: wired Sparkle updater support into the macOS app shell while preserving ad-hoc local build behavior. `AppDelegate` now creates and retains an `SPUStandardUpdaterController` only when `SUFeedURL` is present in the built bundle's `Info.plist`; `MenuBarController` and `PopoverController` thread the optional `SPUUpdater` into Settings. `SettingsView` now shows an "Updates" section only when Sparkle is configured, with "Check for Updates...", automatic update checks, and cadence options for hourly / every 6 hours / daily / weekly. Sparkle imports remain scoped to `Sources/PitwallApp`; no `PitwallAppSupport`, `PitwallShared`, `PitwallCore`, Windows, or Linux targets gained Sparkle imports. Validation: `swift build` passed, `swift test` passed **222 / 222** with 0 failures and 0 regressions, `make build` passed after rerunning outside the sandbox for Swift/Clang user-cache access, and generated ad-hoc `build/Pitwall.app/Contents/Info.plist` has no `SUFeedURL` or `SUPublicEDKey`.
- Phase 6b Step 6b.1 completed: added Sparkle 2.x SwiftPM dependency, hardened runtime entitlements file, and Info.plist Sparkle keys with build script support. Modified `Package.swift` (added `.package(url: "https://github.com/sparkle-project/Sparkle", from: "2.6.0")` dependency, added `"Sparkle"` to `PitwallApp` target, excluded `Pitwall.entitlements` from sources). Created `Sources/PitwallApp/Pitwall.entitlements` (hardened runtime, `com.apple.security.app-sandbox = false`). Modified `Sources/PitwallApp/Info.plist` (added `SUFeedURL` / `SUPublicEDKey` placeholder keys). Modified `scripts/build-app-bundle.sh` to accept `SIGNING_IDENTITY`, `ENTITLEMENTS_PATH`, `SU_FEED_URL`, `SU_PUBLIC_ED_KEY` env vars; substitutes or strips Sparkle placeholders from expanded plist; passes `--entitlements` and `--timestamp` to codesign when appropriate. Sparkle resolved at 2.9.1. Validation: `swift build` clean, `swift test` passes **222 / 222** with 0 failures and 0 regressions, `make install` succeeded with ad-hoc signing (Sparkle keys correctly stripped from plist), `bash scripts/smoke-install.sh` exits 0.
- Phase 6b planned via `/plan-phase 6b`: decomposed macOS Public Release into 9 steps (6b.1–6b.9). Steps 6b.1–6b.5 are automatable with placeholder Sparkle keys (Sparkle SwiftPM dep, AppDelegate/Settings UI wiring, release script, Makefile target, Homebrew cask). Steps 6b.6–6b.8 are blocked on manual prerequisites (Apple Developer enrollment, Developer ID cert, notarytool creds, Sparkle EdDSA key, appcast hosting). Updated `tasks/manual-todo.md` with precise step-level blocker annotations. Test strategy: tests-after. Execution profile: serial.
- Pit road progress indicator for onboarding wizard: replaced the linear `ProgressView` + "Step X of Y" counter with a racing-themed track visualization. Main road always shows 4 fixed nodes (Welcome → Tools → Prefs → Summary); credential steps branch off as a pit road below between Tools and Prefs, animated in/out with `.easeInOut(duration: 0.3)`. Added `OnboardingTrackPosition` type with `Lane` enum (`.main(index:)` / `.pit(index:)`) and `trackPositions(for:)` / `pitProviders(for:)` to `OnboardingWizardStepSequencer`. Created `Sources/PitwallApp/Views/Onboarding/PitRoadProgressView.swift` (custom SwiftUI view using `GeometryReader` + `Path`/`Circle` shapes). Wired into `OnboardingWizardView` with `completedSteps: Set<OnboardingWizardStep>` state for node highlighting. Separate panel styling fix: `PopoverController` now shows "Set Up Pitwall" window title instead of transparent titlebar. 5 new `OnboardingWizardStepSequencerTests` for track positions. Validation: `swift build` clean (no warnings), `swift test` passes **222 / 222** with 0 failures and 0 regressions, `make install` succeeded.

## 2026-04-23

- Onboarding wizard landed (prior session work committed this cut): new SwiftUI wizard split across `Sources/PitwallApp/Views/OnboardingWizardView.swift` and six step views under `Sources/PitwallApp/Views/Onboarding/` (`WelcomeStepView`, `ToolSelectionStepView`, `ClaudeCredentialStepView`, `GenericProviderStepView`, `PreferencesStepView`, `WizardSummaryStepView`), driven by a new `Sources/PitwallAppSupport/OnboardingWizardStepSequencer.swift` (pure, protocol-free step advancement seam). `Sources/PitwallApp/Views/OnboardingView.swift` slimmed from ~110L of legacy flow to a thin host around the wizard. `Tests/PitwallAppSupportTests/OnboardingWizardStepSequencerTests.swift` adds 5 regression tests covering forward/back navigation and completion — Phase 6a baseline moves from 212 → 217.
- Popover dismissal regression fixed: scoped `NSWindow.level = .floating` out of the shared `showWindow(...)` helper in `Sources/PitwallApp/PopoverController.swift` and into the onboarding call site only. `showWindow` now takes `level: NSWindow.Level = .normal`; `showOnboarding` passes `.floating` (wizard still stays on top so the user can ⌘-Tab to Safari while copying `sessionKey` / `lastActiveOrg`); Settings drops back to `.normal`. Why: a same-app `.floating` window stops `.transient` popover dismissal, because clicks to the floating window don't deactivate the app. Scoping fixes both the Settings-forced-on-top bug and the menu-bar popover click-out regression in one change. `.transient` NSPopover flag is untouched — not over-correcting.
- Validation: `swift build` exits 0; `swift test` passes **217 / 217** (212 Phase 6a baseline + 5 new `OnboardingWizardStepSequencerTests`) with 0 failures and 0 regressions. `make build` exits 0; `make install` atomically replaced `/Applications/Pitwall.app` with `codesign --verify --verbose` reporting `valid on disk` + `satisfies its Designated Requirement`. Manual UX pass (5a–5e in the approved plan) is the user's to run post-install.
- Stale-process developer-loop fix: user reported the onboarding panel still detached from the status item after the floating-panel rewrite. Root cause was a stale `PitwallApp` process still running the pre-rewrite binary — `make install` swaps the on-disk bundle but doesn't touch running instances. Edited `Makefile` `install` target to `killall PitwallApp` before copying and `open /Applications/Pitwall.app` after codesign, so the tight dev loop always relaunches the fresh binary. `killall` is prefixed with `-` and suffixed `|| true` so the target stays green when nothing is running.
- Onboarding panel crash fix: after the relaunch, clicking the status item to open then close the wizard crashed with `SIGABRT` during `NSWindow layoutIfNeeded` → `NSHostingView.invalidateSafeAreaInsets`. Two compounding bugs: (a) `MenuBarController.handleStatusItemClick` fell through to `popoverController.toggle(...)` when the onboarding panel was already active, racing an `NSPopover` and an `NSPanel` on the same status-button anchor; (b) `NSPanel` defaulted to `isReleasedWhenClosed = true`, so closing via the X button dangled `onboardingWindowController.window`. Fix in `Sources/PitwallApp/MenuBarController.swift`: when `onboardingPending && onboardingPanelActive`, dismiss the panel instead of toggling the popover. Fix in `Sources/PitwallApp/PopoverController.swift`: set `panel.isReleasedWhenClosed = false`, conform to `NSWindowDelegate`, assign `panel.delegate = self`, and clear `onboardingWindowController` in `windowWillClose(_:)` so X-button closes don't dangle. `Sources/PitwallApp/Views/OnboardingWizardView.swift` frame dropped from 640×720 → 520×580 to match the panel's `onboardingPanelSize`, and the orphaned "Skip setup" copy changed to "Don't show again". `Sources/PitwallApp/Views/OnboardingView.swift` (pre-wizard legacy host, ~20L by this point) deleted — fully superseded by the wizard host in `PopoverController.presentOnboardingPanel(...)`.
- Validation: `swift build` exits 0; `swift test` passes **217 / 217** with 0 failures and 0 regressions (identical to prior session baseline — test-only deltas are unchanged). `make install` atomically killed pid 46356, copied the fresh bundle, codesign reported `valid on disk` + `satisfies its Designated Requirement`, and relaunched as pid 56814 (and later pid from post-crash-fix install). Manual click-to-open / click-to-close UX still the user's to verify post-ship.

## 2026-04-22

- Phase 6a completed and archived to `tasks/phases/phase-6a.md`. Phase 6a Step 6a.11 closed as **"no refactor required"** (analogous to Phase 5 Step 5.8's docs-only close). Re-read the full Phase 6a delta with fresh eyes: `Sources/PitwallAppSupport/PackagingVersion.swift` (72L), `Sources/PitwallAppSupport/LoginItemService.swift` (83L), `Sources/PitwallAppSupport/PackagingProbe.swift` (137L), `Sources/PitwallApp/Views/WelcomeBannerView.swift` (40L), and the integration edits in `Sources/PitwallApp/MenuBarController.swift`, `Sources/PitwallApp/AppDelegate.swift`, `Sources/PitwallApp/PitwallApp.swift`, `Sources/PitwallApp/Views/SettingsView.swift`, `Sources/PitwallApp/Views/PopoverContentView.swift`, plus `scripts/build-app-bundle.sh`, `scripts/smoke-install.sh`, and `Makefile`. All files are single-purpose, protocol-seamed, and free of duplication, dead branches, premature abstraction, or obvious-private types leaked public. `AppDelegate.packagingVersion` is intentionally held as an anchor for a future in-app About section per Step 6a.1 rationale — not dead code. No cheap refactor was worth the churn. Validation: `swift build` exits 0; `swift test` passes **212 / 212** XCTest cases with 0 failures and 0 regressions (identical to the Step 6a.10 baseline — no source changes this step). Grep confirmed no new forbidden imports in `PitwallShared` / `PitwallWindows` / `PitwallLinux`. Packaging scripts were not touched, so `scripts/smoke-install.sh` re-run was not required. Marked Step 6a.11 done in `tasks/todo.md`; ticked all Phase 6a + Milestone acceptance criteria in `tasks/todo.md` and `tasks/roadmap.md`; filled the Phase 6a **On Completion** block in both files; added Phase 6a to the `Completed Phases` index in `tasks/todo.md`; archived the Phase 6a section to `tasks/phases/phase-6a.md`. Phase 6a macOS Local Install milestone closes. Next priority-queue item is `/plan-phase 6b`, deferred until the author confirms intent to distribute publicly (blocked on Apple Developer enrollment + Sparkle/notary credentials).
- Phase 6a Step 6a.10 completed: ran the final acceptance gate for the Phase 6a packaging milestone — full test suite + packaging smoke checks + visual install/uninstall confirmation. No source changes this step. Validation commands executed in sequence: `swift build` exited 0 (clean); `swift test` passed **212 / 212** XCTest cases with 0 failures and 0 regressions (193 Phase 5 baseline + 19 Phase 6a tests from Step 6a.9) — this confirms the Phase 6a baseline test count is **212**. `bash scripts/smoke-install.sh` exited 0 (`smoke-install: OK`; `CFBundleShortVersionString=1.0.0`, `CFBundleVersion=96`; `codesign --verify --verbose build/Pitwall.app` → `valid on disk` + `satisfies its Designated Requirement`). `make build` exited 0 and `codesign --verify --verbose build/Pitwall.app` reported `valid on disk` + `satisfies its Designated Requirement`. `open build/Pitwall.app` launched `build/Pitwall.app/Contents/MacOS/PitwallApp` (PID 76683) and the user visually confirmed the menu bar shows the `gauge.with.dots.needle.67percent` SF Symbol. Under explicit user authorization, `make install` atomically replaced `/Applications/Pitwall.app` (`rm -rf` then `cp -R` then `codesign --verify --verbose`); codesign on the installed copy reported `valid on disk` + `satisfies its Designated Requirement`; `open /Applications/Pitwall.app` launched PID 78298 and the user visually confirmed the SF Symbol in the menu bar again. Under a second explicit user authorization, `make uninstall` invoked `/Applications/Pitwall.app/Contents/MacOS/PitwallApp --unregister-login-item` then `rm -rf /Applications/Pitwall.app`; post-uninstall `ls /Applications/Pitwall.app` fails with `No such file or directory` (as expected), `ls ~/Library/Application Support/Pitwall/` succeeds (directory preserved, empty — no provider configuration was ever created in this session, so no state to preserve but the directory itself remains intact), and `security find-generic-password -s com.pitwall.provider-secrets` returns `SecKeychainSearchCopyNext: The specified item could not be found` (no provider credential was ever saved in this session, so the Keychain entry was never created — `make uninstall` did not touch Keychain items, which is the contract being validated). Grep confirmed no new `import AppKit` / `import UserNotifications` / `import Security` in `PitwallShared` / `PitwallWindows` / `PitwallLinux` (pre-existing `import Security` in `Sources/PitwallCore/KeychainSecretStore.swift` is a Phase 2 artifact, not a Phase 6a addition — `git log` confirms it dates to commit `9c7fbeb`). Phase 6a Step 6a.11 (refactor slot) queued next; if 6a.11 has no refactor opportunity, the Phase 6a milestone closes.

- Phase 6a Step 6a.9 completed: backfilled XCTest coverage for the Phase 6a code paths that shipped tests-after (Steps 6a.1 / 6a.5 / 6a.6). Created `Tests/PitwallAppSupportTests/PackagingVersionTests.swift` — asserts `StaticPackagingVersionProvider` round-trips its configured value, the two init overloads converge, `BundlePackagingVersionProvider` falls back to the unbundled constants (`"0.0.0-dev"` / `0`) when the injected bundle lacks `CFBundleShortVersionString` / `CFBundleVersion`, honors a custom fallback primed from the on-disk `VERSION` file (reads the repo `VERSION` via `#filePath` walk and checks the semver regex + `build > 0`), and `PackagingVersion` equality is correct. Created `Tests/PitwallAppSupportTests/LoginItemServiceTests.swift` — exercises `InMemoryLoginItemService` for default-disabled / initially-enabled state, enable/disable transitions, `setEnabledCallCount` tracking every call (4 after two enables + two disables), injected-error propagation (`LoginItemServiceError.requiresApproval` surfaces without mutating state), clear-error-then-succeed, and asserts `LoginItemServiceError.registrationFailed` / `.unregistrationFailed` `errorDescription` carries the underlying `NSError` message. Created `Tests/PitwallAppSupportTests/PackagingProbeTests.swift` — builds a per-test throwaway app-support root under `NSTemporaryDirectory()` with `addTeardownBlock` cleanup, isolated `UserDefaults(suiteName:)`, and a unique-keyed `DiagnosticEventStore`; verifies first `runOnce` writes exactly two `DiagnosticEvent`s (`appSupportProbe` with `writable=true` + path, `keychainProbe` with `roundTripSucceeded=true`) and flips the first-launch key, second `runOnce` is a no-op (returns nil, event count stays at 2), writing a regular file at the app-support path forces `createDirectory` to fail and surfaces `appSupportWritable=false` + non-nil `error` details, a `MismatchingSecretStore` private actor triggers `keychainRoundTripSucceeded=false` with `error="roundTripMismatch"`, and a `ThrowingSecretStore` private actor surfaces a non-nil error string from the `save` throw path. Reused Phase 2's `InMemorySecretStore` (no duplicate fixture). No welcome-banner test: `PopoverContentView.swift` uses `@AppStorage` directly with no view-model seam, matching the plan's "skip if no cheap seam exists" branch. No production source changes — test-only delta.
- Validation: `swift build` passes. `swift test` passes **212 / 212** XCTest cases with 0 failures and 0 regressions — 193 Phase 5 baseline + 19 new Phase 6a tests (5 `PackagingVersionTests` + 9 `LoginItemServiceTests` + 5 `PackagingProbeTests`). `bash scripts/smoke-install.sh` exits 0 (`smoke-install: OK`; `CFBundleShortVersionString=1.0.0`, `CFBundleVersion=94`; `codesign --verify --verbose` reports `valid on disk` + `satisfies its Designated Requirement`). Grep confirmed no new `import AppKit` / `import UserNotifications` / `import Security` / cross-module leakage in `PitwallShared` / `PitwallCore` / `PitwallWindows` / `PitwallLinux` — this step is test-only and only adds imports of `XCTest`, `@testable import PitwallAppSupport`, and `PitwallCore` inside `Tests/PitwallAppSupportTests/`. Phase 6a test baseline is now 212 and carries forward into Step 6a.10.

- Phase 6a Step 6a.8 completed: landed `scripts/smoke-install.sh` as the shell-form packaging-artifact validator that exercises the Step 6a.2 pipeline without touching `/Applications/` or launching the app. Created `scripts/smoke-install.sh` (executable, `set -euo pipefail`; `cd`s to repo root), which: (a) invokes `bash scripts/build-app-bundle.sh` (already idempotent — self-cleans `build/Pitwall.app`), (b) asserts `test -x build/Pitwall.app/Contents/MacOS/PitwallApp`, (c) reads `CFBundleShortVersionString` via `defaults read` and requires it to match `^[0-9]+\.[0-9]+\.[0-9]+$` (rejects the literal `{{CFBundleShortVersionString}}` placeholder), (d) reads `CFBundleVersion` via `defaults read` and requires it to match `^[0-9]+$` (rejects the `{{CFBundleVersion}}` placeholder), (e) runs `codesign --verify --verbose build/Pitwall.app` and exits non-zero on failure, and (f) prints `smoke-install: OK` on success. Does not run `make install`, does not touch `/Applications/`, does not launch the app, does not call `--unregister-login-item`. The regex gates catch the Step 6a.2 placeholder-survival failure mode before a broken bundle can reach `make install` in Step 6a.10.
- Test strategy: tests-after. Step 6a.8 IS a test in shell form; no XCTest coverage ships — the Phase 6a XCTest additions all live in Step 6a.9 (`PackagingVersionTests`, `LoginItemServiceTests`, `PackagingProbeTests`).
- Validation: `bash scripts/smoke-install.sh` exits 0 (`smoke-install: OK`; `CFBundleShortVersionString=1.0.0`, `CFBundleVersion=92`). `swift build` passes. `swift test` passes **193/193** XCTest cases with 0 failures and 0 regressions — byte-identical to the Step 6a.7 baseline. `make build` exits 0 and produces `build/Pitwall.app`; `codesign --verify --verbose build/Pitwall.app` reports `valid on disk` + `satisfies its Designated Requirement`. No Swift source changes in this step — trivially no new forbidden imports in `PitwallShared` / `PitwallCore` / `PitwallWindows` / `PitwallLinux`.

- Phase 6a Step 6a.7 completed: landed the first-launch "Welcome to Pitwall" banner inside the popover. Created `Sources/PitwallApp/Views/WelcomeBannerView.swift` — a pure SwiftUI presentational view with an `onDismiss: () -> Void` caller-supplied callback; renders a hand-wave SF Symbol, the three required copy blocks verbatim ("Welcome to Pitwall. You're replacing a previous menu bar app — Pitwall does not copy data from it." / "Paste your Claude `sessionKey` and `lastActiveOrg` in Settings → Claude account to get started." / "Secrets are stored in the macOS Keychain."), and a borderless `xmark.circle.fill` dismiss button; card layout matches the existing `actionSummary` tile (`controlBackgroundColor` + `RoundedRectangle(cornerRadius: 8)`); no network, no side effects, no `UserDefaults` / model access inside the view itself. Modified `Sources/PitwallApp/Views/PopoverContentView.swift` to add an `@AppStorage("pitwall.welcome.v1.dismissed") private var welcomeDismissed: Bool = false` gate and conditionally render `WelcomeBannerView(onDismiss: { welcomeDismissed = true })` directly under the header and above `actionSummary` when the key is `false`; SwiftUI re-render on `@AppStorage` mutation collapses the banner for the rest of the launch and the set key suppresses it on subsequent launches forever. Key name `pitwall.welcome.v1.dismissed` mirrors the Step 6a.5 `pitwall.onboarding.completed.v1` and Step 6a.6 `pitwall.packaging.probe.firstLaunch.v1` convention. No new SwiftPM target, no linker changes, no platform-shell leakage.
- Test strategy: tests-after. Step 6a.7 ships no XCTest coverage of its own — per `tasks/todo.md` Step 6a.9 the banner's `UserDefaults` gate is exercised indirectly later if a small view-model seam is cheap; in this cut the banner is a pure SwiftUI view on top of `@AppStorage`, so no extra seam was introduced. Visual confirmation deferred to Step 6a.10.
- Validation: `swift build` passes. `swift test` passes **193/193** XCTest cases with 0 failures and 0 regressions — byte-identical to the Step 6a.6 baseline. `make build` exits 0 and produces `build/Pitwall.app`; `codesign --verify --verbose build/Pitwall.app` reports `valid on disk` + `satisfies its Designated Requirement`. Grep confirmed no new `AppKit` / `SwiftUI` / `Cocoa` imports in `PitwallShared` / `PitwallCore` / `PitwallWindows` / `PitwallLinux` (the new SwiftUI import lives only in `PitwallApp`).

- Phase 6a Step 6a.6 completed: added the first-launch Application Support + Keychain health probe so packaging-install regressions are captured on the very first run. Created `Sources/PitwallAppSupport/PackagingProbe.swift` with (a) `PackagingProbeResult` (`appSupportWritable: Bool`, `appSupportError: String?`, `keychainRoundTripSucceeded: Bool`, `keychainError: String?`), (b) `PackagingProbe` taking an injectable `FileManager`, an app-support root `URL`, a `ProviderSecretStore`, a disposable `ProviderSecretKey` (default `providerId: packaging-probe`, `accountId: probe`, `purpose: probe` — never collides with production `ProviderSecretKey` values), and a `@Sendable () -> Date` clock seam, and (c) `runOnce(eventStore:defaults:firstLaunchKey:)` which short-circuits on an already-set `UserDefaults` key (`pitwall.packaging.probe.firstLaunch.v1`) and otherwise: creates the app-support directory with intermediates, writes + removes a `.packaging-probe` file to prove writability, runs a UUID save/load/mismatch-check/delete cycle through the injected secret store, appends two `DiagnosticEvent`s (summaries `"appSupportProbe"` and `"keychainProbe"` with the result booleans + optional error strings in `details`) via `DiagnosticEventStore.append`, and flips the `UserDefaults` key. Wired into `Sources/PitwallApp/MenuBarController.swift`: `start()` calls `runPackagingProbeIfNeeded()` exactly once alongside `loadConfiguration()`, dispatching to `Task.detached` so the probe never blocks status-bar bring-up. The probe's Keychain store is a dedicated `KeychainSecretStore(service: "com.pitwall.app.packaging-probe")`, which keeps disposable probe items entirely separate from the production `com.pitwall.provider-secrets` keychain service. All probe events still pass through `DiagnosticsRedactor`; no network I/O; no upload path. Tests for the new probe are deferred to Step 6a.9 per the Phase 6a red/green decomposition.
- Validation: `swift build` passes, `swift test` passes the full 193-test baseline with zero regressions, `make build` assembles `build/Pitwall.app` cleanly and `codesign --verify --verbose build/Pitwall.app` reports `valid on disk` + `satisfies its Designated Requirement`, and `grep` confirms no new forbidden `AppKit` / `PitwallAppSupport` imports leaked into `PitwallShared` / `PitwallCore` / `PitwallWindows` / `PitwallLinux`.

- Phase 6a Step 6a.5 completed: wired macOS Launch-at-Login through `SMAppService.mainApp`, exposed the toggle in Settings, and made `--unregister-login-item` a real CLI flag so `make uninstall` can cleanly unregister before removing the bundle. Created `Sources/PitwallAppSupport/LoginItemService.swift` with (a) the `LoginItemService` protocol (`isEnabled: Bool` + `setEnabled(_:) throws`), (b) `SMAppServiceLoginItemService` wrapping `SMAppService.mainApp` (maps `status == .enabled` to `isEnabled`; `register()` / `unregister()` translated through a `LoginItemServiceError` enum with a dedicated `.requiresApproval` case when post-register status flips to `requiresApproval`), (c) `InMemoryLoginItemService` fixture for Step 6a.9's regression tests (supports `setEnabledError` injection + `setEnabledCallCount` observation), and (d) `LoginItemServiceError` as `LocalizedError` so the Settings view can show a friendly message. Modified `Sources/PitwallApp/Views/SettingsView.swift` to accept an injected `LoginItemService?`, surface a "Startup" section with a "Launch Pitwall at login" `Toggle` wired through the service, and show an inline secondary-style error string if `setEnabled` throws (no new alert infrastructure). Modified `Sources/PitwallApp/PopoverController.swift` to thread `loginItemService` through `showSettings` to `SettingsView`. Modified `Sources/PitwallApp/MenuBarController.swift` to own a `LoginItemService` member (injection-friendly initializer; defaults to `SMAppServiceLoginItemService()` on macOS 13+, `InMemoryLoginItemService()` otherwise) and pass it through `presentSettings()`. Rewrote `Sources/PitwallApp/PitwallApp.swift` to intercept `CommandLine.arguments.contains("--unregister-login-item")` before any AppKit setup: if present, call `SMAppService.mainApp.unregister()` synchronously (best-effort; `try?`) and `exit(0)`. `Package.swift` gained `.linkedFramework("ServiceManagement")` on the `PitwallApp` executable target's `linkerSettings`. Protocol-seam + in-memory-fixture pattern matches `ProviderSecretStore` / `NotificationScheduling` / `PackagingVersionProvider`; no new SwiftPM target.
- Test strategy: tests-after. Step 6a.5 ships no XCTest coverage of its own — `LoginItemServiceTests` (binding `InMemoryLoginItemService` to assert toggle behavior + idempotency) land in Step 6a.9. The `SMAppService`-backed implementation and the `--unregister-login-item` CLI flag are exercised end-to-end in Step 6a.10 (reboot-after-enable + `make uninstall` visual pass).
- Validation: `swift build` passes. `swift test` passes **193/193** XCTest cases with 0 failures and 0 regressions — byte-identical to the Step 6a.4 baseline. `make build` exits 0 and produces `build/Pitwall.app` with substituted `CFBundleShortVersionString=1.0.0` / `CFBundleVersion=85`; `codesign --verify --verbose build/Pitwall.app` exits 0 (`valid on disk` + `satisfies its Designated Requirement`) after the new `ServiceManagement` linked framework. Grep verified `import ServiceManagement` appears only in `Sources/PitwallAppSupport/LoginItemService.swift` and `Sources/PitwallApp/PitwallApp.swift` — zero leakage into `PitwallShared` / `PitwallCore` / `PitwallWindows` / `PitwallLinux`.

- Phase 6a Step 6a.4 completed: wired the menu bar status item to an SF Symbol glyph instead of the formatted capacity text. Modified `Sources/PitwallApp/MenuBarController.swift` to (a) assign `NSImage(systemSymbolName: "gauge.with.dots.needle.67percent", accessibilityDescription: "Pitwall")` to `statusItem?.button?.image` in `start()` alongside the existing click-target wiring, and (b) move the `MenuBarStatusFormatter`-derived status string from `button.title` to `button.toolTip` in `updateStatusTitle()` so the capacity hint remains discoverable on hover but no longer occupies menu bar width. No asset catalog / `.imageset` / `.icns` added — SF Symbol only, reserved upgrade slot for Phase 6b. No `Package.swift` change. No changes to `showContextMenu()` — its inline title item still uses the formatter output, so the dropdown header continues to show the capacity summary. Popover click targeting (`target`, `action`, `sendAction(on:)`) preserved unchanged.
- Test strategy: tests-after. Step 6a.4 ships no XCTest coverage of its own — the SF Symbol wiring is exercised end-to-end in Step 6a.10 (`make install` → visual confirmation that `/Applications/Pitwall.app` shows the `gauge.with.dots.needle.67percent` glyph in the menu bar and the tooltip surfaces the capacity summary). `MenuBarController` remains outside XCTest coverage (its collaborators are tested via their own protocols; the controller is AppKit-bound).
- Validation: `swift build` passes. `swift test` passes **193/193** XCTest cases with 0 failures and 0 regressions — byte-identical to the Step 6a.3 baseline. `make build` exits 0 and produces `build/Pitwall.app` with substituted `CFBundleShortVersionString=1.0.0` / `CFBundleVersion=83`. Grep confirmed no new forbidden imports: `import AppKit` was already present in `MenuBarController`, no AppKit leakage into `PitwallShared` / `PitwallAppSupport` / platform shells.

- Phase 6a Step 6a.3 completed: landed the top-level `Makefile` that wraps the Step 6a.2 packaging pipeline. Created `Makefile` at the repo root with tab-indented recipes and `.PHONY: build install uninstall run clean`. `make build` shells out to `bash scripts/build-app-bundle.sh`; `make install` depends on `build`, atomically replaces `/Applications/Pitwall.app` (`rm -rf` then `cp -R`), runs `codesign --verify --verbose /Applications/Pitwall.app`, and prints the "Open Pitwall from /Applications — it will appear in your menu bar." guidance; `make uninstall` invokes `/Applications/Pitwall.app/Contents/MacOS/PitwallApp --unregister-login-item` prefixed with `-` so an unrecognized-flag exit (Step 6a.5 adds the flag) does not fail the target, then `rm -rf /Applications/Pitwall.app`, then prints that Application Support (`~/Library/Application Support/Pitwall/`) and Keychain items are preserved; `make run` depends on `build` and `open build/Pitwall.app`; `make clean` removes `build/`. No build logic lives in the `Makefile` — it is an invocation shell on top of `scripts/build-app-bundle.sh` from Step 6a.2.
- Test strategy: tests-after. Step 6a.3 ships no XCTest coverage of its own — the `Makefile` is exercised end-to-end in Step 6a.10 alongside the login-item round-trip.
- Validation: `swift build` passes. `swift test` passes **193/193** XCTest cases with 0 failures and 0 regressions — byte-identical to the Step 6a.2 baseline (no source changes this step). `make build` exits 0 and produces `build/Pitwall.app` with substituted `CFBundleShortVersionString=1.0.0` / `CFBundleVersion=81` via the Step 6a.2 script. `make clean` removes `build/`; `ls build/` then fails as expected. `make install` / `make uninstall` were deliberately not executed this step — they mutate `/Applications/` and are deferred to Step 6a.10 where the full login-item round-trip lands. Grep confirmed no new `import AppKit` / `import UserNotifications` / `import Security` in forbidden modules (trivial — no source changes).

- Phase 6a Step 6a.2 completed: landed the unsigned-in-the-formal-sense but ad-hoc-signed `.app` bundle wrapper that turns `swift build --configuration release --product PitwallApp` output into `build/Pitwall.app`. Created `scripts/build-app-bundle.sh` (`set -euo pipefail`; idempotent via `rm -rf build/Pitwall.app` before reassembly; reads `VERSION` from disk for `CFBundleShortVersionString` — no hard-coded `1.0.0`; computes `CFBundleVersion` from `git rev-list --count HEAD`; constructs `Contents/{MacOS,Resources}`; copies the built binary to `Contents/MacOS/PitwallApp`; `sed`-substitutes `{{CFBundleShortVersionString}}` / `{{CFBundleVersion}}` placeholders in the `Info.plist` template into `Contents/Info.plist`; guards against unsubstituted placeholders surviving; ad-hoc-signs with `codesign --sign - --deep --force --options=runtime`). The script resolves the built-binary path via `swift build --show-bin-path` rather than a hard-coded `.build/release/` so it is robust to SwiftPM layout changes. Modified `Sources/PitwallApp/Info.plist` to add `CFBundleExecutable=PitwallApp`, `NSHumanReadableCopyright=Copyright © 2026 Pitwall contributors. All rights reserved.`, and the two placeholder keys `CFBundleShortVersionString={{CFBundleShortVersionString}}` + `CFBundleVersion={{CFBundleVersion}}`; preserved existing `LSUIElement=true`, `LSMinimumSystemVersion=13.0`, `CFBundleName`, `CFBundleDisplayName`, `CFBundleIdentifier`, `CFBundlePackageType`. `Package.swift:62` already excludes `Info.plist` from the `PitwallApp` executable target, so the literal template placeholders do not break `swift build`. `BundlePackagingVersionProvider` from Step 6a.1 now reads the substituted values back out of `Bundle.main` at runtime — file → bundle → app loop closed.
- Test strategy: tests-after. Step 6a.2 ships no XCTest coverage of its own — the packaging path is exercised end-to-end in Step 6a.10 (`make build` → `open build/Pitwall.app`); `PackagingProbeTests` (Step 6a.9) cover in-process probe logic, not the shell script.
- Validation: `swift build` passes. `swift test` passes **193/193** XCTest cases with 0 failures and 0 regressions — byte-identical to the Step 6a.1 baseline. `bash scripts/build-app-bundle.sh` exits 0 and produces `build/Pitwall.app`; `codesign --verify --verbose build/Pitwall.app` exits 0 (`valid on disk` + `satisfies its Designated Requirement`); `plutil -p build/Pitwall.app/Contents/Info.plist` confirms `CFBundleShortVersionString = "1.0.0"` and `CFBundleVersion = "78"` (both placeholders substituted; build number equals `git rev-list --count HEAD`). Re-running the script is idempotent (tested — no stale state). Grep confirmed no new `import AppKit` / `import UserNotifications` / `import Security` lines appeared in forbidden modules (`PitwallShared`, `PitwallCore`, `PitwallAppSupport`); the only pre-existing hits (`PitwallCore/KeychainSecretStore.swift` and `PitwallAppSupport/NotificationScheduler.swift`) predate Phase 6a.

- Phase 6a Step 6a.1 completed: landed the single source of truth for the macOS app version before any bundling code ships. Created `VERSION` at the repo root with initial content `1.0.0` (the canonical `CFBundleShortVersionString` anchor that Step 6a.2's `scripts/build-app-bundle.sh` will substitute into `Info.plist`; `CFBundleVersion` is still build-time-computed via `git rev-list --count HEAD` in Step 6a.2, not here). Added `Sources/PitwallAppSupport/PackagingVersion.swift` with (a) a `PackagingVersion` value type (`shortString: String`, `build: Int`, `Equatable + Sendable`), (b) a `PackagingVersionProvider` protocol exposing `current() -> PackagingVersion`, (c) a `StaticPackagingVersionProvider` fixture for tests, and (d) a `BundlePackagingVersionProvider` that reads `CFBundleShortVersionString` + `CFBundleVersion` from an injected `Bundle` (defaults to `.main`) at runtime — no hard-coded version string anywhere in the binary. The bundle provider falls back to `0.0.0-dev` / `0` when running unbundled (e.g. `swift run`) so dev builds do not claim a real release version. Wired `AppDelegate` to accept an injected `PackagingVersionProvider` (default = `BundlePackagingVersionProvider()`) and read the current version at `applicationDidFinishLaunching` into a `packagingVersion` property for the Step 6a follow-up About view to consume. Protocol-seam + in-memory-fixture pattern mirrors `ProviderSecretStore` (Phase 2) and `NotificationScheduling` (Phase 4) per reuse anchors. No new SwiftPM target — `PitwallAppSupport` already has the right dependency fan-in (`PitwallCore` + `PitwallShared`).
- Test strategy: tests-after (Step 6a.9 writes `PackagingVersionTests` alongside `LoginItemServiceTests` and `PackagingProbeTests`). No tests landed in this step.
- Validation: `swift build` passes. `swift test` passes **193/193** XCTest cases with 0 failures and 0 regressions — byte-identical to the Phase 5 baseline. Grep verified `PackagingVersion.swift` imports only `Foundation` (no `AppKit` / `Security` / `UserNotifications`); no new forbidden imports appeared in `PitwallShared` / `PitwallWindows` / `PitwallLinux`.

- Phase 5 Cross-Platform V1 Parity milestone closed and archived. `tasks/todo.md` was copied to `tasks/phases/phase-5.md`; the Phase 5 milestone in `tasks/roadmap.md` is now fully checked with on-completion deviations (CI gap documented) and follow-ups (real platform bindings + CI runners + production backend wiring behind existing seams) recorded. A fresh `tasks/todo.md` records the completed-roadmap status, the post-v1 follow-up list, and a pointer to run `/roadmap` for the next concrete work item. Pitwall v1 five-phase roadmap is complete; no Phase 6 is defined.
- Phase 5 Step 5.8 completed: ran the cross-platform boundary refactor audit against `PitwallShared` / `PitwallWindows` / `PitwallLinux` / `PitwallAppSupport` and recorded the outcome as **"no refactor required"**. Docs-only step — no `Package.swift`, no `Sources/**`, and no `Tests/**` source files were edited. The Windows/Linux apparent symmetry (`*StatusFormatter`, `*TrayMenuViewModel` + `*TrayMenuBuilder`, `Stored*` Codable types, `*ClaudeCredential{Input,Error}`, `*NotificationScheduler`, `*DiagnosticsExporter`) is an intentional platform-isolation boundary that the Step 5.6 parity regression tests bind to via hard-coded `Expected` string constants in both suites — collapsing to a shared type would make the parity tests compare a type to itself and erase a real cross-shell drift signal. `PitwallAppSupport` pure-Foundation files (`MenuBarStatusFormatter`, `ProviderCardViewModel`, `PollingPolicy`, `ProviderRotationController`, `ProviderConfigurationStore`, `ProviderHistoryStore`, etc.) stay in AppSupport: they are consumed only by the macOS `PitwallApp` shell, bind to macOS paths and `UserDefaults`, and the Step 5.2 extraction already moved every cross-platform-worthy piece (`NotificationPolicy`, `NotificationPreferences`, `UserPreferences`, `ProviderConfiguration`, and the `ProviderConfigurationStorage` / `ProviderHistoryStorage` / `SettingsStorage` / `NotificationScheduling` protocols) into `PitwallShared`.
- Architecture doc updated: added "Cross-Platform Boundary Refactor Audit (Step 5.8)" section to `docs/cross-platform-architecture.md` enumerating each candidate considered (per-shell formatters, tray view models, stored Codable types, credential flow input/error, notification scheduler adapters, diagnostics exporters, `Expected` test fixtures, AppSupport pure files) and the reason each stays where it is. Privacy fences re-verified by grep: no `import AppKit` / `import UserNotifications` / `import Security` in `PitwallShared` or platform shells; no cross-shell imports between `PitwallWindows` and `PitwallLinux`; neither imports `PitwallAppSupport`. No new SwiftPM targets — the current six-target layout (`PitwallCore` / `PitwallShared` / `PitwallAppSupport` / `PitwallApp` / `PitwallWindows` / `PitwallLinux`) is the final Phase 5 shape.
- Phase 5 milestone acceptance marked complete in `tasks/todo.md`. Phase 5 archival is prepared for the next session (deferred per the Step 5.8 handoff contract — do not archive in this step).
- Windows / Linux CI gap carried forward unchanged: no Windows or Linux toolchain / CI host is wired up in this repo, so `swift build --triple x86_64-unknown-windows-msvc` + `swift test` on a real Windows host and `swift build` + `swift test` on a real Linux host were **not** executed. Language mirrors 5.3 / 5.4 / 5.5 / 5.6 / 5.7 precedent.
- Validation: `swift build` passes. `swift test` passes **193 XCTest cases with 0 failures and 0 regressions** — byte-identical to the Step 5.7 baseline, as expected of a docs-only audit outcome. No source or test code was changed this step.

- Phase 5 Step 5.7 completed: ran the Phase 5 milestone validation gate against the supported build toolchain. Verification-only step — no source or test code changed. `swift build` passes. `swift test` executed **193 XCTest cases with 0 failures and 0 regressions**, matching the Step 5.6 baseline exactly (167 pre-5.6 + 26 new 5.6 suites: 5 shared + 11 Windows + 10 Linux). macOS host: Apple Swift 6.2.4 on `arm64-apple-macosx26.0`, Darwin 25.3.0. `PitwallCore`, `PitwallShared`, `PitwallAppSupport`, `PitwallApp`, `PitwallWindows`, and `PitwallLinux` all compile and their test targets all run cleanly under macOS; macOS test runs continue to exercise `PitwallWindowsTests` and `PitwallLinuxTests` as portability proxies (pure Foundation, no platform-specific bindings yet).
- Windows / Linux CI gap recorded as a platform limitation, not silently relaxed: no Windows or Linux toolchain / CI host is wired up in this repo, so `swift build --triple x86_64-unknown-windows-msvc` + `swift test` on a real Windows host and `swift build` + `swift test` on a real Linux host were **not** executed. Language mirrors 5.3 / 5.4 / 5.5 / 5.6 precedent.
- Architecture doc updated: added "Platform Validation (Step 5.7)" section to `docs/cross-platform-architecture.md` recording (a) macOS commands run + exact test counts + pass status, (b) Windows / Linux CI gap explicitly carried forward, (c) the Phase 5 milestone acceptance ↔ Step 5.6 regression coverage map (linking back rather than duplicating), and (d) the explicit "not yet validated on real platform hosts" list (Win32 `Shell_NotifyIcon` tray, WinRT `ToastNotificationManager`, production Credential Manager path, `libsecret` / Secret Service production path, `libnotify` / D-Bus delivery, `libayatana-appindicator` binding + "no tray available" fallback, real `FindFirstFileW` / `stat(2)` filesystem probes, end-to-end tray + notification UX).
- Phase 5 milestone acceptance checklist in `tasks/todo.md` updated to reflect verified-on-macOS vs. documented-platform-limitation status per bullet; the "On Completion" section captures the CI-gap deviation and the concrete follow-ups (real platform bindings + CI runners + production backend wiring behind the existing seams). The Step 5.8 plan now has a concrete scope (audit cross-platform boundaries for refactor candidates; behavior-preserving only; may be docs-only if the audit clears).
- Validation: `swift build` passes. `swift test` passes 193 XCTest cases with 0 failures and no regressions. No source or test code was changed this step.

- Phase 5 Step 5.6 completed: landed tests-only cross-platform regression suites pinning the Phase 5 acceptance criteria against the `PitwallShared` / `PitwallCore` / `PitwallWindows` / `PitwallLinux` public surfaces shipped in 5.2 / 5.3 / 5.4 / 5.5. Added `Tests/PitwallSharedTests/CrossPlatformRegressionTests.swift` (shared anchor covering provider-visibility filtering, `ProviderHistoryRetention` windowing + hour-bucket downsampling, `DiagnosticsRedactor` token key set, and the `GitHubHeatmapResponseMapper` recorded fixture); `Tests/PitwallWindowsTests/WindowsCrossPlatformRegressionTests.swift` and `Tests/PitwallLinuxTests/LinuxCrossPlatformRegressionTests.swift` (per-platform mirrors covering round-trip Claude/Codex/Gemini enable + selection with disabled providers hidden from the tray view model, byte-identical tooltips + card labels via hard-coded `Expected` constants, credential write-only + `backendUnavailable` enum visibility, Codex/Gemini presence-only sanitization with no absolute path / `sk-` / `ghp_` / `ya29.` / `AIza` substrings and no fabricated evidence when the probe is suppressed, diagnostics redaction parity via `DiagnosticsRedactor`, history retention parity on a shared fixture, and identical `GitHubHeatmapResponseMapper` output for the recorded fixture). Parity is enforced without a cross-shell import by pinning both platform suites to the same `Expected` string constants — drift on either side fails the sibling test.
- Architecture doc updated: added "Cross-Platform Regression Coverage (Step 5.6)" section to `docs/cross-platform-architecture.md` mapping each Phase 5 acceptance bullet to the covering suite and recording constraints honored (no new production code, no new SwiftPM targets, no forbidden imports, tmp-root fixtures only). CI gap carried forward from 5.3 / 5.4 / 5.5 is restated with a note that Step 5.7 inherits it, and a "Gap kept open for Step 5.7 / 5.8" line explicitly lists the real Win32 / WinRT / `libsecret` / `libnotify` / `libayatana-appindicator` bindings and production Credential Manager / Secret Service paths as not-yet-validated on real platform hosts.
- Validation: `swift build` passes. `swift test` passes 193 XCTest cases (167 pre-existing + 26 new — 5 shared + 11 Windows + 10 Linux) with 0 failures and no regressions. Grep verified no `import AppKit` / `import UserNotifications` / `import Security` / `import PitwallAppSupport` in the new test files; Linux regression tests do not import `PitwallWindows` and vice versa. Fixture roots are injected tmp directories; no test touches the user's real home, `%APPDATA%`, or XDG paths.
- Phase 5 Step 5.5 completed: added platform-specific Codex/Gemini passive detection adapters under `PitwallWindows` and `PitwallLinux`. Four new detectors (`WindowsCodexDetector`, `WindowsGeminiDetector`, `LinuxCodexDetector`, `LinuxGeminiDetector`), each taking an injected storage root (`WindowsStorageRoot` / `LinuxStorageRoot` mirrors) and a narrow presence-only probe seam (`WindowsCodexFilesystemProbing`, `WindowsGeminiFilesystemProbing`, `LinuxCodexFilesystemProbing`, `LinuxGeminiFilesystemProbing`) — production wires to a filesystem reader in a follow-up packaging step; tests inject fixture probes. Per-platform suppressed fallbacks (`WindowsCodexSuppressedProbe` / `WindowsGeminiSuppressedProbe` / `LinuxCodexSuppressedProbe` / `LinuxGeminiSuppressedProbe`) return `suppressed: true` evidence with empty artifacts so the shell surfaces the degraded state rather than fabricating presence signals. Evidence types (`WindowsCodexArtifactMetadata`, `WindowsCodexDetectorEvidence`, and Gemini / Linux siblings) carry only existence + byte size + mtime; sanitizers pin `relativePath` to the caller-supplied name and clamp byte size to non-negative so a misconfigured probe cannot echo absolute paths or token-shaped substrings back. No file-content reads; token bytes never enter memory.
- Path maps recorded in `docs/cross-platform-architecture.md` under a new "Codex/Gemini Passive Detection (Step 5.5)" section: Windows Codex under `%APPDATA%\Codex\`, Windows Gemini under `%APPDATA%\Gemini\` (`%LOCALAPPDATA%` not used — Codex/Gemini CLIs write identity + history under the roaming profile); Linux Codex under `$XDG_CONFIG_HOME/codex/` with `~/.config/codex/` fallback, Linux Gemini under `$XDG_CONFIG_HOME/gemini/` with `~/.config/gemini/` fallback (`$XDG_DATA_HOME` not used). XDG env overrides are honored only at the shell boundary; the detector protocol takes an already-resolved root. Unsupported metadata sources (per-session `tokenCount` parsing, log-file rate-limit hint scanning) are explicitly out of scope for Step 5.5's presence-only contract and punted to a future opt-in reader.
- CI gap recorded: no Windows / Linux CI host is configured yet. Because every detector is pure Foundation, macOS `swift test` runs `PitwallWindowsTests` + `PitwallLinuxTests` as a portability proxy (mirroring Step 5.3 / 5.4 precedent). Wiring real `FindFirstFileW` (Windows) and `stat(2)` (Linux) bindings + platform CI runners is a known follow-up.
- Validation: `swift build` passes. `swift test` passes 167 XCTest cases (144 pre-existing + 23 new detector tests — 11 Windows + 12 Linux across Codex and Gemini) with 0 failures and no regressions. Grep verified no `import AppKit` / `import UserNotifications` / `import Security` / `import PitwallAppSupport` in the new detector sources; no cross-shell imports (`PitwallWindows` detectors do not import `PitwallLinux` and vice versa). Sanitization tests assert no absolute path (`C:\`, `/Users/`, `/home/`) and no token-shaped substring (`sk-`, `ya29.`) appears in returned evidence on either platform; suppressed-probe fail-closed tests confirm no evidence is fabricated when the probe is unavailable; injected-root tests confirm the detector never creates or writes to any real user directory.

- Phase 5 Step 5.4 completed: introduced the `PitwallLinux` SwiftPM target (dependency `PitwallCore` + `PitwallShared`, no `PitwallAppSupport`, no `PitwallWindows`) holding Linux adapters against the Step 5.2 contracts. Adapters: `LinuxStorageRoot` with `xdgConfig` / `xdgData` resolvers that honor `XDG_CONFIG_HOME` / `XDG_DATA_HOME` overrides at the shell boundary and fall back to `~/.config/pitwall/` / `~/.local/share/pitwall/` — the protocol itself takes an injected root; file-backed JSON `LinuxProviderConfigurationStore`, `LinuxProviderHistoryStore`, `LinuxSettingsStore` with atomic writes and auto-created directories; `LinuxNotificationScheduler` plugging into `PitwallShared.NotificationScheduling` via a narrow `LinuxNotificationDelivering` backend (prod = `libnotify` / `org.freedesktop.Notifications` D-Bus TBD, tests = spy, documented suppressed fallback = `LinuxNotificationSuppressedBackend`); `LinuxSecretServiceStore` conforming to `ProviderSecretStore` via a narrow `LinuxSecretServiceBackend` seam (prod = `libsecret` / Secret Service API TBD, tests = `InMemoryLinuxSecretBackend`). Secret store fails closed: `save` throws `LinuxSecretServiceError.backendUnavailable` when the backend is unavailable; `loadSecret` returns `nil` rather than a degraded plaintext default. UI surface landed as pure view-model types: `LinuxStatusFormatter`, `LinuxProviderCardViewModel`, `LinuxTrayMenuViewModel`, `LinuxTrayMenuBuilder`, `LinuxClaudeCredentialFlow` (manual Claude capture, session-key write-only), `LinuxDiagnosticsExporter` (redacted JSON bundle via `PitwallCore.DiagnosticsExportBuilder`). All files use only `Foundation` + `PitwallCore` + `PitwallShared`; no AppKit/UserNotifications/Security imports, no dependency on `PitwallAppSupport` or `PitwallWindows`. Added `Tests/PitwallLinuxTests` (32 tests) with fixture-driven round-trip, secret-store fail-closed, notification-policy wiring, manual Claude flow, diagnostics redaction, and tray view-model coverage — no live Secret Service, no live D-Bus delivery, no real XDG dirs.
- Architecture doc updated: added "Linux Shell Stack (Step 5.4)" section to `docs/cross-platform-architecture.md` recording tray toolkit (`libayatana-appindicator` with windowed-popover fallback for sessions lacking indicator support), notification backend (`libnotify` / D-Bus + `LinuxNotificationSuppressedBackend` fallback), secret-service fallback copy ("Secure storage unavailable — sign-in disabled" settings banner, no plaintext file fallback), storage layout under XDG paths, and the CI-gap status mirroring Step 5.3.
- CI gap recorded: no Linux CI host is configured yet, so `swift build` + `swift test` on a real Linux toolchain are not yet part of the gate. Because every Linux adapter is pure Foundation, macOS `swift test` runs `PitwallLinuxTests` as a portability proxy (mirroring the Step 5.3 approach); wiring real `libsecret` / `libnotify` / `libayatana-appindicator` bindings + a Linux CI runner is called out as a follow-up.
- Validation: `swift build` passes. `swift test` passes 144 XCTest cases (112 pre-existing + 32 new `PitwallLinuxTests`) with 0 failures and no regressions. Grep verified no `import AppKit`/`import UserNotifications`/`import Security`/`import PitwallAppSupport`/`import PitwallWindows` in `Sources/PitwallLinux/`. Secret-store fail-closed asserted by test: backend-unavailable → `save` throws `backendUnavailable`, `loadSecret` returns `nil`.
- Phase 5 Step 5.3 completed: introduced the `PitwallWindows` SwiftPM target (dependency `PitwallCore` + `PitwallShared`, no `PitwallAppSupport`) holding Windows adapters against the Step 5.2 contracts. Adapters: `WindowsStorageRoot` (injected root so `%APPDATA%\Pitwall\` is resolved by the shell, not the protocol); file-backed JSON `WindowsProviderConfigurationStore`, `WindowsProviderHistoryStore`, `WindowsSettingsStore` with atomic writes and auto-created directories; `WindowsNotificationScheduler` that plugs into `PitwallShared.NotificationScheduling` via a narrow `WindowsToastDelivering` backend (prod = WinRT toast binding TBD, tests = spy, doc'd suppressed fallback = `WindowsToastSuppressedBackend`); `WindowsCredentialManagerSecretStore` conforming to `ProviderSecretStore` via a narrow `WindowsCredentialManagerBackend` seam (prod = `CredWriteW`/`CredReadW`/`CredDeleteW`, tests = `InMemoryWindowsCredentialBackend`, fails closed when writes are disabled). UI surface landed as pure view-model types: `WindowsStatusFormatter`, `WindowsProviderCardViewModel`, `WindowsTrayMenuViewModel`, `WindowsTrayMenuBuilder`, `WindowsClaudeCredentialFlow` (manual Claude capture, session key write-only), `WindowsDiagnosticsExporter` (redacted JSON bundle via `PitwallCore.DiagnosticsExportBuilder`). All files use only `Foundation` + `PitwallCore` + `PitwallShared`; no AppKit/UserNotifications/Security imports, no dependency on `PitwallAppSupport`. Added `Tests/PitwallWindowsTests` (28 tests) with fixture-driven round-trip and view-model coverage — no live Credential Manager, no live toast delivery, no real `%APPDATA%`.
- CI gap recorded: no Windows CI host is available, so `swift build --triple x86_64-unknown-windows-msvc` and `swift test` on a real Windows toolchain are not yet part of the gate. Because every Windows adapter is pure Foundation, the macOS build runs `PitwallWindowsTests` as a portability proxy; wiring to real Win32 symbols is still required in a future Windows-shell packaging step and is called out as a follow-up.
- Validation: `swift build` passes. `swift test` passes 112 XCTest cases (84 pre-existing + 28 new `PitwallWindowsTests`) with 0 failures and no regressions. Grep verified no `import AppKit`/`import UserNotifications`/`import PitwallAppSupport`/`import Security` in `Sources/PitwallWindows/`.
- Phase 5 Step 5.2 completed: introduced the `PitwallShared` SwiftPM target holding cross-platform contracts (`ProviderConfigurationStorage`, `ProviderHistoryStorage`, `SettingsStorage`) plus portable value types (`UserPreferences`, `NotificationPreferences`, `ProviderProfileConfiguration`, `ClaudeAccountConfiguration`, `ProviderConfigurationSnapshot`, `ResetDisplayPreference`, `ProviderRotationMode`) and pure policy (`NotificationPolicy`, `NotificationKind`, `NotificationEvent`, `NotificationRequest`, `NotificationScheduling`). `PitwallShared` depends only on `PitwallCore` and imports no AppKit/UserNotifications. `PitwallAppSupport` now depends on `PitwallShared`, re-exports it via `@_exported import`, and has its existing `ProviderConfigurationStore`/`ProviderHistoryStore` actors conform to the new storage protocols; on-disk UserDefaults layout and all macOS behavior unchanged. Added `Tests/PitwallSharedTests` with in-memory fakes and round-trip/policy coverage.
- Validation: `swift build` passes. `swift test` passes 84 XCTest cases (74 pre-existing + 10 new `PitwallSharedTests`) with 0 failures and no regressions. Grep verified no `import AppKit`/`import UserNotifications` in `Sources/PitwallShared/`.
- Phase 5 Step 5.1 completed: selected SwiftPM-first with platform-specific shells as the cross-platform architecture and documented it in `docs/cross-platform-architecture.md` (module layout, adapter seams for `ProviderSecretStore`/`NotificationScheduling`/storage/tray, per-platform privacy guardrails, build/test strategy, deferred decisions for Steps 5.2–5.5). Added a `### Cross-Platform Plans` subsection to `README.md` linking to the doc without claiming Windows/Linux support works yet. No `Package.swift` or source changes; architecture lane ran in an isolated worktree with a strict diff scope (`README.md` + `docs/cross-platform-architecture.md`).
- Validation: `swift build` passes. `swift test` passes 74 XCTest cases with 0 failures and no regressions.

## 2026-04-21

- Phase 4 completed: all milestone acceptance criteria are satisfied, the completed phase was archived to `tasks/phases/phase-4.md`, and Phase 5 Cross-Platform V1 Parity was planned just-in-time in `tasks/todo.md` and `tasks/roadmap.md`.
- Phase 4 Step 4.8 completed: reviewed v1 hardening boundaries after the green pass and kept the existing architecture intact because pure retention/redaction/GitHub request logic is already in `PitwallCore`, app persistence/coordination is in `PitwallAppSupport`, and SwiftUI/AppKit presentation is in `PitwallApp`.
- Removed the obsolete `Sources/PitwallCore/RedPhaseStub.swift` red-phase placeholder now that the core target has real implementation files. Review-only lane completed locally due subagent policy.
- Validation: `swift test` passes 74 XCTest cases with 0 failures and no warnings emitted. `swift build` passes with no warnings emitted.
- Phase 4 Step 4.7 completed: ran the full Phase 4 green validation and fixed review-lane privacy/retention findings before shipping the step.
- Diagnostics redaction now handles separator-style secret/raw keys such as `raw_response`, `model_response`, `session_key`, and `source_content`, and redacts fine-grained GitHub personal access tokens with the `github_pat_` prefix.
- Configured history retention is now enforced when loading/pruning persisted derived snapshots, and provider history store APIs accept explicit maximum retention intervals with regression coverage.
- Validation: `swift test` passes 74 XCTest cases with 0 failures and no warnings emitted. `swift build` passes with no warnings emitted.
- Phase 4 Step 4.6 completed: wired Phase 4 history, diagnostics, notifications, and optional GitHub heatmap state into the macOS app surface.
- Replaced the popover history placeholder with compact derived-history sparklines, added a redacted diagnostics export view, surfaced optional GitHub contribution heatmap data, and persisted Phase 4 settings plus write-only GitHub token saves through the app controller.
- Notification scheduling now flows through the app-level refresh path with duplicate suppression, and disabled history retention clears/suppresses retained derived history display.
- Validation: `swift test` passes 72 XCTest cases with 0 failures and no warnings emitted. `swift build` passes with no warnings emitted.
- Phase 4 Step 4.5 completed: added optional GitHub heatmap core request/response models, GraphQL-variable request construction, 12-week response mapping, hourly automatic refresh policy with manual bypass, invalid-token mapping for 401/403, and write-only token management through `ProviderSecretStore`.
- Added Phase 4 settings persistence for history, diagnostics, notifications, and non-secret GitHub heatmap preferences; GitHub personal access tokens stay out of `UserDefaults`. Added a native GitHub heatmap settings section that accepts new token input without rendering saved token values.
- Validation: `swift build` passes with no warnings emitted. `swift test` passes 72 XCTest cases with 0 failures and no warnings emitted.
- Phase 4 Step 4.4 completed: added configurable notification preferences, deterministic notification policy decisions, a scheduler protocol, a macOS `UserNotificationScheduler`, settings persistence for notification preferences, and a native settings section for reset/auth/degraded/pacing-threshold notifications.
- Notification scheduling is testable through injected schedulers and the concrete macOS scheduler is isolated behind `NotificationScheduling`, so tests do not require live notification permission.
- Validation: `swift build` passes with no warnings emitted. `swift test` remains red as expected because later Phase 4 red tests still reference unimplemented GitHub heatmap and Phase 4 settings symbols (`GitHubHeatmapRequest`, `GitHubHeatmapTokenManager`, `Phase4SettingsStore`, and related models); SwiftPM compiles those files before filtered notification tests can execute.
- Phase 4 Step 4.3 completed: added deterministic diagnostics redaction/export models, local redacted diagnostic event storage, diagnostics export assembly, and refresh coordinator diagnostic events for Claude auth/network failures plus Codex/Gemini passive scan failures.
- Diagnostics redaction now strips sensitive keys and token/account-id-like values before persistence/export, and refresh outcomes no longer include the Claude account id in expired-auth diagnostics.
- Validation: `swift build` passes with no warnings emitted. `swift test` remains red as expected because later Phase 4 red tests still reference unimplemented notification, settings, and GitHub heatmap symbols (`NotificationPolicy`, `Phase4SettingsStore`, `GitHubHeatmapRequest`, and related models).
- Phase 4 Step 4.2 completed: added `ProviderHistorySnapshot`, deterministic history retention/downsampling, a derived-only app-support history store, and Claude refresh coordinator integration that persists sanitized usage history after successful refreshes.
- History retention keeps all snapshots for the last 24 hours, downsamples 24-hour-to-7-day snapshots into hourly buckets, preserves highest session utilization and latest weekly utilization per bucket, and drops snapshots older than seven days.
- Validation: `swift build` passes with no warnings emitted. `swift test --filter HistoryRetentionTests` still fails before execution as expected because SwiftPM compiles later red-phase Phase 4 test files with missing diagnostics, notification, settings, and GitHub heatmap symbols. A direct module-level history retention check passed.
- Phase 4 Step 4.1 completed: added red XCTest coverage for v1 hardening history retention/downsampling, derived-only history snapshots, diagnostics redaction/export metadata, configurable notification policy behavior, GitHub heatmap GraphQL/rate-limit/token-state behavior, and Phase 4 settings persistence without storing GitHub tokens in `UserDefaults`.
- Validation: `swift test` fails as expected for the red phase because Phase 4 implementation symbols do not exist yet (`ProviderHistorySnapshot`, `ProviderHistoryRetention`, `DiagnosticsRedactor`, `GitHubHeatmapTokenManager`, `NotificationPolicy`, `Phase4SettingsStore`, and related models). No syntax or package-wiring issues were identified beyond expected missing-symbol diagnostics.
- Phase 3 completed: all milestone acceptance criteria are satisfied, the completed phase was archived to `tasks/phases/phase-3.md`, and Phase 4 V1 Hardening, History, Diagnostics, Notifications, And GitHub Heatmap was planned just-in-time in `tasks/todo.md` and `tasks/roadmap.md`.
- Phase 3 Step 3.9 completed: audited app boundaries and moved provider-state construction out of `MenuBarController` into a testable `ProviderStateFactory` in `PitwallAppSupport`.
- Added app-support tests for provider visibility, skipped-provider configurability, passive state preservation, Claude credential privacy labels, expired refresh disabling, and sanitized placeholder state. Review-only lane completed locally due subagent policy.
- Validation: `swift test` passes 56 XCTest cases with 0 failures and no warnings emitted. `swift build` passes with no warnings emitted.
- Phase 3 Step 3.8 completed: ran macOS app validation for the first usable provider parity phase and confirmed all Phase 1-3 regression tests and app target compilation pass cleanly.
- Validation: `swift test` passes 51 XCTest cases with 0 failures and no warnings emitted. `swift build` passes with no warnings emitted. No source changes were needed.
- Phase 3 Step 3.7 completed: added a `PitwallAppSupportTests` target with regression coverage for menu bar formatting, reset display, provider rotation, provider card view models, provider configuration persistence, Claude credential setup privacy, and refresh coordination privacy boundaries.
- Refresh coordination tests use injected fake Claude clients, local snapshot loaders, secret stores, and deterministic clocks; no live provider networks or real user provider files are read. Saved Claude session keys remain write-only, replacement keys rotate through the secret store only, expired auth is preserved, and stale Claude state keeps only non-secret last-success metadata.
- Validation: `swift test` passes 51 XCTest cases with 0 failures and no warnings emitted. `swift build` passes with no warnings emitted. Review-only lane completed locally due subagent policy.
- Phase 3 Step 3.6 completed: added native onboarding and settings windows for provider enablement, Claude manual credential setup, display preferences, tray rotation, manual refresh, and test connection wiring through the existing configuration, secret-store, and refresh coordinator boundaries. Saved Claude session keys remain write-only in UI state and skipped providers stay visible as configurable cards.
- Validation: `swift build` passes. `swift test` passes 29 XCTest cases with 0 failures and no warnings emitted. Review-only lane completed locally due subagent policy.
- Phase 3 Step 3.5 completed: added injectable Claude usage refresh, polling/backoff policy, sanitized Codex/Gemini local snapshot loading, and provider refresh coordination through the existing configuration and secret-store boundaries.
- Validation: `swift build` passes. `swift test` passes 29 XCTest cases with 0 failures and no warnings emitted.
- Phase 3 Step 3.4 completed: added a macOS Keychain-backed `ProviderSecretStore`, public configured/missing/expired secret state, app-support non-secret provider configuration persistence, and Claude account setup flows that save/replace/delete session keys without rendering saved credential values.
- Validation: `swift build` passes. `swift test` passes 29 XCTest cases with 0 failures and no warnings emitted.
- Phase 3 Step 3.3 completed: added a controller-backed macOS status item, transient popover, native SwiftUI provider cards, status badges, Claude usage rows, action summary, and refresh/settings/add-account controls using sanitized sample provider state.
- Validation: `swift build` passes. `swift test` passes 29 XCTest cases with 0 failures and no warnings emitted.
- Phase 3 Step 3.2 completed: added app-support provider state, display preferences, provider card view models, compact menu bar status formatting, and deterministic provider rotation with pinned/manual/paused behavior and degraded-provider skipping.
- Validation: `swift build` passes. `swift test` passes 29 XCTest cases with 0 failures and no warnings emitted.
- Phase 3 Step 3.1 completed: added SwiftPM `PitwallAppSupport` and `PitwallApp` targets, a minimal AppKit menu bar status item shell, accessory/no-Dock activation behavior, and an `LSUIElement` plist for future app bundling.
- Validation: `swift build` passes. `swift test` passes 29 XCTest cases with 0 failures and no warnings emitted.
- Phase 2 Step 2.8 completed: audited provider foundation boundaries and kept the step as a no-code refactor because shared `ProviderModels.swift` remains provider-agnostic while Claude, Codex, Gemini, confidence, and secret-store behavior stay isolated in adapter-specific files.
- Validation: `swift test` passes 29 XCTest cases with 0 failures and no warnings emitted.
- Phase 2 completed: all milestone acceptance criteria are satisfied, the completed phase was archived to `tasks/phases/phase-2.md`, and Phase 3 First Usable macOS Provider Parity was planned just-in-time in `tasks/todo.md` and `tasks/roadmap.md`.
- Phase 2 Step 2.7 completed: ran the provider foundation green verification and confirmed Claude parsing, Codex/Gemini detection, provider confidence, secret-store, daily budget, and pacing tests all pass.
- Validation: `swift test` passes 29 XCTest cases with 0 failures and no warnings emitted.
- Phase 2 Step 2.6 completed: added `ProviderConfidenceMapper` with sanitized evidence inputs for Claude exact usage, Codex passive evidence, Gemini passive evidence, degraded telemetry fallback, and missing configuration states.
- Validation: `swift test --filter ProviderConfidenceTests` passes 5 confidence tests. Full `swift test` passes 29 XCTest cases with 0 failures and no warnings emitted.
- Phase 2 Step 2.5 completed: added `GeminiLocalDetector` for injected `LocalProviderFileSnapshot` evidence, reporting safe install/auth/activity booleans, sanitized settings metadata, and observed token counts without reading the real filesystem or serializing OAuth tokens or raw chat content.
- Validation: `swift build` passes. `swift test` and `swift test --filter ProviderDetectionTests` still fail as expected during the red phase because Step 2.6 has not implemented `ProviderConfidenceMapper`; SwiftPM compiles the confidence test file before running filtered tests.
- Phase 2 Step 2.4 completed: added injected `LocalProviderFileSnapshot` evidence helpers and a `CodexLocalDetector` that reports safe install/config/auth/activity/rate-limit booleans without reading the real filesystem or serializing auth tokens, prompts, stdout, source content, or raw session text.
- Validation: `swift build` passes. `swift test` progresses past Codex detector symbols and remains red as expected on later Phase 2 missing symbols (`GeminiLocalDetector` and `ProviderConfidenceMapper`).
- Phase 2 Step 2.3 completed: added `ProviderSecretKey`, async `ProviderSecretStore`, write-only `ProviderSecretState`, and an actor-backed `InMemorySecretStore` for injected tests without adding production Keychain calls.
- Adjusted secret-store XCTest assertions to await values before calling XCTest autoclosures, preserving the write-only public-state privacy check.
- Validation: `swift build` passes. `swift test` and `swift test --filter SecretStoreTests` both still fail as expected during the red phase because SwiftPM compiles all test files and later Phase 2 symbols are not implemented yet (`ProviderConfidenceMapper`, `LocalProviderFileSnapshot`, `CodexLocalDetector`, and `GeminiLocalDetector`).
- Phase 2 Step 2.2 completed: added Claude account/usage models, tolerant Claude usage parsing for known sections/null sections/unknown keys/extra usage, UTC reset parsing, and Claude auth/network error normalization into non-secret `ProviderState` values.
- Validation: `swift build` passes after allowing SwiftPM to write its compiler cache. `swift test` progresses past Claude parser tests and remains red as expected on later Phase 2 missing symbols (`InMemorySecretStore`, `ProviderSecretKey`, `ProviderSecretState`, `LocalProviderFileSnapshot`, `CodexLocalDetector`, `GeminiLocalDetector`, and `ProviderConfidenceMapper`).
- Phase 2 Step 2.1 completed: added red XCTest coverage for Claude usage parsing/error normalization, Codex and Gemini passive detection sanitization, provider confidence mapping, and write-only secret-store behavior, plus Claude JSON fixtures.
- Validation: `swift test` fails as expected for the red phase because Phase 2 implementation symbols do not exist yet (`ClaudeUsageParser`, `CodexLocalDetector`, `GeminiLocalDetector`, `ProviderConfidenceMapper`, `InMemorySecretStore`, and related models). No SwiftPM fixture warnings are emitted after declaring test resources.
- Phase 1 Step 1.6 completed: kept provider model public names stable, refactored pacing-window configuration into private calculator internals, and added explicit weekly ratio boundary tests for `0.50`, `0.85`, `1.15`, `1.50`, and `2.00`.
- Validation: baseline `swift test` passed 10 XCTest cases before edits; final `swift test` passes 11 XCTest cases with 0 failures and no warnings emitted.
- Phase 1 completed: all milestone acceptance criteria are satisfied, the completed phase was archived to `tasks/phases/phase-1.md`, and Phase 2 Provider Data Foundations was planned just-in-time in `tasks/todo.md` and `tasks/roadmap.md`.
- Phase 1 Step 1.5 completed: ran the core Swift test suite and verified all current Phase 1 pacing tests pass.
- Validation: `swift test` passes 10 XCTest cases with 0 failures and no warnings emitted.
- Phase 1 Step 1.4 completed: added a lightweight `PitwallCore` module anchor and updated `README.md` with the current SwiftPM target, `swift test` usage, and clean-room input rules for future app targets.
- Validation: `swift test` passes 10 XCTest cases with no warnings emitted.
- Phase 1 Step 1.3 completed: implemented `PacingCalculator` with deterministic weekly/session pace evaluation, ignore-window handling, capped/action mapping, daily budget calculation, and today's usage baseline behavior.
- Corrected daily-budget test fixtures so UTC calendar tests straddle an actual UTC local-midnight boundary while preserving coverage for exact, estimated, and unknown today-usage states.
- Validation: `swift test` passes 10 tests with no warnings emitted.
- Phase 1 Step 1.2 completed: added provider-agnostic core models for provider identity/status/confidence, pacing labels, recommendation actions, reset windows, usage snapshots, today's usage, pace evaluation, daily budget, normalized pacing state, provider actions, and provider-specific payload escape hatches.
- Validation: `swift test` builds the `PitwallCore` target with the new model layer and fails as expected because Step 1.3 has not implemented `PacingCalculator` yet.
- Phase 1 Step 1.1 completed: added a SwiftPM manifest, a red-phase `PitwallCore` target placeholder, and XCTest coverage for weekly/session pacing thresholds, ignore windows, capped utilization, daily budget, and today's usage baseline behavior.
- Validation: `swift test` reaches test compilation and fails as expected because the Step 1.2/1.3 core symbols are not implemented yet (`PacingCalculator`, `PacingLabel`, `RecommendedAction`, `UsageSnapshot`).
